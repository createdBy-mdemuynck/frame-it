# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: frame-it
metadata:
  template: frame-it@0.0.1-beta

services:
  backoffice:
    project: ./server
    language: js
    host: containerapp
    docker:
      path: ./Dockerfile
      context: ./

  frontoffice:
    project: ./web
    language: js
    host: containerapp
    docker:
      path: ./Dockerfile
      context: ./
